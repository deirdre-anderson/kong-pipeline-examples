_format_version: "1.1"
_workspace: Team1
services:
- connect_timeout: 60000
  host: httpbin.org
  name: httpbin
  path: /anything
  port: 80
  protocol: http
  read_timeout: 60000
  retries: 5
  write_timeout: 60000
consumers:
- custom_id: user2
  username: user2
  keyauth_credentials:
  - key: key1
plugins:
- name: key-auth
  config:
    anonymous: null
    hide_credentials: false
    key_in_body: false
    key_in_header: true
    key_in_query: true
    key_names:
    - apikey
    run_on_preflight: true
  enabled: true
  protocols:
  - grpc
  - grpcs
  - http
  - https
